<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.purposeless.tfthelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Champions.ChampionHolderGUI.ChampionDetailActivity"></activity>
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".Items.ItemQuizGUI.ItemQuizActivity" />
        <activity android:name=".Champions.ChampionHolderGUI.ChampionHolderActivity" />
        <activity android:name=".Champions.ChampionBuilderGUI.ChampionBuilderActivity" />
        <activity android:name=".Items.ItemInventoryGUI.ItemActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>